{
  "name": "swig-ts",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "sideEffects": true,
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun --filter '*' build",
    "build:classic": "bun --filter '@swig/classic' build",
    "build:coder": "bun --filter '@swig/coder' build",
    "build:doc": "typedoc",
    "changesets": "changeset",
    "dev": "bun --filter '*' dev",
    "dev:classic": "bun --filter '@swig/classic' dev",
    "dev:coder": "bun --filter '@swig/coder' dev",
    "format": "prettier --check packages/",
    "format:fix": "prettier --write packages/",
    "release": "bun run build && changeset publish"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.3",
    "@types/node": "^22.13.10",
    "@typescript-eslint/eslint-plugin": "^8.26.1",
    "@typescript-eslint/parser": "^8.26.1",
    "eslint": "^9.22.0",
    "eslint-config-prettier": "^10.1.1",
    "prettier": "^3.3.3",
    "prettier-plugin-organize-imports": "^4.0.0",
    "ts-node": "^10.9.2",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@ethereumjs/wallet": "^2.0.4",
    "@solana/spl-token": "^0.4.13",
    "@solana/web3.js": "^1.98.0",
    "ethers": "^6.13.5",
    "ws": "^8.18.1"
  }
}